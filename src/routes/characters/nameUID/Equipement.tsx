import { Character } from "../../../data/Characters";

/**
 * Equipement tab.
 */
export default function Equipement(props: { character: Character }) {
  return <div className="bg-indigo-950">Equipement</div>;
}
