import { Routes } from "react-router-dom";

/**
 * Page entrypoint.
 */
export default function Index() {
  // Build all routes

  // Render
  return (
    <div>
      <Routes></Routes>
    </div>
  );
}
